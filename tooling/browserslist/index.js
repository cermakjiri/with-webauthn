/**
 * @url https://browsersl.ist/#q=defaults+and+fully+supports+es6-module+and+%3E0.2%25%2C+not+iOS+%3C+16%2C+safari+%3E%3D+16
 */
module.exports = `defaults and fully supports es6-module and >0.2%, not iOS < 16, safari >= 16`.split(', ');
